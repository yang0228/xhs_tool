version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: xhs
      POSTGRES_PASSWORD: xhs_dev
      POSTGRES_DB: xhs_tool
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U xhs -d xhs_tool"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
